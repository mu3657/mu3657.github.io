---
---

<div class="fixed top-0 left-0 w-full h-full -z-1 overflow-hidden pointer-events-none">
    <!-- Flare Layer (Screen blend mode) -->
    <!-- Added 'animate-pulse-slow' for breathing effect -->
    <div class="fixed top-0 left-0 w-full h-full z-1 mix-blend-screen opacity-60 animate-pulse-slow pointer-events-none">
        <img class="absolute top-0 left-0 w-full h-full object-cover" src="/images/background-flare.jpg" alt="Background Flare" />
    </div>

    <!-- Noise/Grain Layer (Optional, if we have texture) -->
    <!-- <div class="fixed top-0 left-0 w-full h-full z-0 opacity-20 pointer-events-none mix-blend-overlay" style="background-image: url('/images/scratch-bg_afhtpy.png');"></div> -->
</div>

<style>
    @keyframes pulse-slow {
        0%, 100% { opacity: 0.4; }
        50% { opacity: 0.8; }
    }
    .animate-pulse-slow {
        animation: pulse-slow 8s ease-in-out infinite;
    }
</style>
